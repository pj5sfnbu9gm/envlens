// Package differ provides utilities for comparing environment variable maps.
//
// The Projection feature allows callers to narrow diff results to a focused
// subset of keys — either by exact name or by key prefix. Results can also
// be inverted to exclude matched keys instead of including them.
package differ
