// Package differ provides utilities for comparing environment variable maps.
//
// The Annotate function enriches diff results with human-readable labels
// based on the change status (added, removed, changed, unchanged) and
// optional per-key notes supplied by the caller.
package differ
